{
	"name": "auto-vault-functions",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev src/index.ts",
		"build": "wrangler deploy --dry-run --outdir=dist",
		"deploy": "wrangler deploy",
		"lint": "biome check .",
		"format": "biome format --write ."
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240411.0",
		"wrangler": "^4.12.0"
	}
}
